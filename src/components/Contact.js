const Contact = ({mail,number}) => {
    return (
        <div className="contact">
            <h1>Mail: {mail}</h1>
            <h1>Number: {number}</h1>
        </div>
    )
}

export default Contact